diff --git i/Makefile w/Makefile
index 56c19e2..28fc1c2 100644
--- i/Makefile
+++ w/Makefile
@@ -2,6 +2,7 @@ PREFIX ?= /usr
 SYSCONFDIR ?= /etc
 DESTDIR ?=
 DSP_SOURCE ?=
+WITH_QWEN_TTS ?= 0
 QWEN_TTS_BINARY ?=
 QWEN_TTS_LICENSE ?=
 GGML_LICENSE ?=
@@ -9,21 +10,31 @@ GGML_LICENSE ?=
 PACKAGE_SHARE := $(DESTDIR)$(PREFIX)/share/omarchy-t2
 PACKAGE_LIB := $(DESTDIR)$(PREFIX)/lib/omarchy-t2
 
+ifneq ($(WITH_QWEN_TTS),0)
+ifneq ($(WITH_QWEN_TTS),1)
+$(error WITH_QWEN_TTS must be 0 or 1)
+endif
+endif
+
 .PHONY: install test
 
 install:
 	@test -n "$(DSP_SOURCE)" || { echo "DSP_SOURCE must point to t2-apple-audio-dsp speakers_161" >&2; exit 1; }
 	@test -f "$(DSP_SOURCE)/config/10-t2_161_speakers.conf"
+ifeq ($(WITH_QWEN_TTS),1)
 	@test -x "$(QWEN_TTS_BINARY)" || { echo "QWEN_TTS_BINARY must point to a Vulkan qwen-tts executable" >&2; exit 1; }
 	@test -f "$(QWEN_TTS_LICENSE)" || { echo "QWEN_TTS_LICENSE must point to qwentts.cpp's license" >&2; exit 1; }
 	@test -f "$(GGML_LICENSE)" || { echo "GGML_LICENSE must point to ggml's license" >&2; exit 1; }
+endif
 	install -Dm755 bin/omarchy-t2 "$(DESTDIR)$(PREFIX)/bin/omarchy-t2"
 	install -Dm755 libexec/omarchy-tts "$(DESTDIR)$(PREFIX)/bin/omarchy-tts"
 	install -Dm755 libexec/omarchy-tts-queue.py "$(PACKAGE_LIB)/tts-queue"
 	install -Dm755 libexec/omarchy-t2-power-optimizer "$(PACKAGE_LIB)/power-optimizer"
 	install -Dm755 libexec/omarchy-t2-fan-control "$(PACKAGE_LIB)/fan-control"
 	install -Dm755 libexec/omarchy-t2-audio-output-sink "$(PACKAGE_LIB)/audio-output-sink"
+ifeq ($(WITH_QWEN_TTS),1)
 	install -Dm755 "$(QWEN_TTS_BINARY)" "$(PACKAGE_LIB)/qwen-tts"
+endif
 	install -Dm755 libexec/omarchy-t2-apply-battery-limit "$(PACKAGE_LIB)/apply-battery-limit"
 	install -Dm755 libexec/omarchy-t2-bluetooth-agent "$(PACKAGE_LIB)/bluetooth-agent"
 	install -Dm755 libexec/omarchy-t2-mic-guard "$(PACKAGE_LIB)/mic-guard"
@@ -52,11 +63,14 @@ install:
 	install -m644 "$(DSP_SOURCE)"/firs/macbook_pro_t2_16_1_*_4-96k.wav "$(DESTDIR)$(PREFIX)/share/pipewire/devices/apple/"
 	install -Dm644 LICENSE "$(DESTDIR)$(PREFIX)/share/licenses/omarchy-t2/LICENSE"
 	install -Dm644 "$(DSP_SOURCE)/LICENSE" "$(DESTDIR)$(PREFIX)/share/licenses/omarchy-t2/t2-apple-audio-dsp-LICENSE"
+ifeq ($(WITH_QWEN_TTS),1)
 	install -Dm644 "$(QWEN_TTS_LICENSE)" "$(DESTDIR)$(PREFIX)/share/licenses/omarchy-t2/qwentts.cpp-LICENSE"
 	install -Dm644 "$(GGML_LICENSE)" "$(DESTDIR)$(PREFIX)/share/licenses/omarchy-t2/ggml-LICENSE"
+endif
 	install -Dm644 README.md "$(DESTDIR)$(PREFIX)/share/doc/omarchy-t2/README.md"
 
 test:
+	bash tests/test-status.sh
 	python3 tests/test-tts-queue.py
 	bash tests/test-audio-output-sink.sh
 	bash tests/test-power-optimizer.sh
diff --git i/README.md w/README.md
index d2327c0..6305c31 100644
--- i/README.md
+++ w/README.md
@@ -33,7 +33,7 @@ omarchy-t2 setup --dry-run
   and temperature-aware CPU idle time
 - Protected six-channel speaker DSP for `MacBookPro16,1`
 - Normalized, limited mono microphone DSP using the working array channel
-- Bluetooth passkey display and A2DP auto-connect
+- Optional Bluetooth passkey display and A2DP auto-connect
 - Compatibility helper for Omarchy's T2 GPU toggle
 - Optional React DRM Touch Bar control center from the AminMarashi fork
 - Optional Qwen3-TTS reading on the Radeon through Vulkan
@@ -62,12 +62,20 @@ omarchy-t2 input keyboard us mac-iso
 omarchy-t2 input tap on
 omarchy-t2 audio speakers off
 omarchy-t2 audio mic on
-omarchy-t2 bluetooth disable
+omarchy-t2 bluetooth enable
 omarchy toggle hybrid gpu
 omarchy-t2 touchbar setup
 omarchy-t2 tts setup
 ```
 
+Bluetooth changes are opt-in: use `omarchy-t2 bluetooth enable` separately,
+or `omarchy-t2 setup --bluetooth` to include them during setup. Ordinary setup
+and restore leave unmanaged Bluetooth configuration alone.
+
+Enabling audio DSP preserves existing audio profiles and Omarchy speaker tuning.
+Files replaced at omarchy-t2's own managed paths are still backed up. Restore
+also supports profile backups created by earlier versions of this command.
+
 ## Touch Bar
 
 Install the React DRM Touch Bar control center from
@@ -136,8 +144,15 @@ including its confirmation and reboot flow.
 
 ## GPU text-to-speech
 
-Run the optional setup once to download and verify the Q4 models (about 1.4
-GB):
+Qwen TTS is excluded from the default installation, so installing the command
+does not compile anything. To bundle the Vulkan engine, build the AUR package
+with `WITH_QWEN_TTS=1 makepkg -si` (this explicitly enables compilation).
+Alternatively, set `OMARCHY_T2_TTS_ENGINE` to an existing Vulkan `qwen-tts`
+executable and install the optional playback dependencies listed by the package.
+Keep that environment variable set when running setup and playback.
+
+Once the engine is installed, run setup to download and verify the Q4 models
+(about 1.4 GB):
 
 ```bash
 omarchy-t2 tts setup
@@ -159,6 +174,12 @@ The setup unbinds those keys before installing its bindings, stores models in
 Disable the bindings without deleting models with `omarchy-t2 tts disable`.
 Remove the downloaded models with `omarchy-t2 tts remove`.
 
+`omarchy-t2 status` reads the hardware battery limit, live service state,
+Hyprland input options and TTS bindings, and available audio DSP nodes. Fan
+profiles are identified from the installed fan configuration and labeled as
+such. Unreachable services report `unavailable` instead of saved defaults.
+`omarchy-t2 power status` labels saved policy settings as `configured.*`.
+
 Inspect the machine with `omarchy-t2 status` and `omarchy-t2 doctor`. Restore
 the files that were present before setup with:
 
@@ -215,12 +236,15 @@ To stage a complete package tree, pass a checkout of the speaker DSP branch:
 ```bash
 make DESTDIR=/tmp/omarchy-t2-stage \
   DSP_SOURCE=/path/to/t2-apple-audio-dsp \
-  QWEN_TTS_BINARY=/path/to/qwen-tts \
-  QWEN_TTS_LICENSE=/path/to/qwentts.cpp/LICENSE \
-  GGML_LICENSE=/path/to/qwentts.cpp/ggml/LICENSE \
   install
 ```
 
+This copies scripts and audio assets without compiling. To include an existing
+Qwen engine, additionally pass `WITH_QWEN_TTS=1`,
+`QWEN_TTS_BINARY=/path/to/qwen-tts`,
+`QWEN_TTS_LICENSE=/path/to/qwentts.cpp/LICENSE`, and
+`GGML_LICENSE=/path/to/qwentts.cpp/ggml/LICENSE`.
+
 ## Author
 
 Amin Marashi <me@amin.codes>
diff --git i/bin/omarchy-t2 w/bin/omarchy-t2
index 8b83951..114b9a8 100755
--- i/bin/omarchy-t2
+++ w/bin/omarchy-t2
@@ -34,7 +34,7 @@ usage() {
 Omarchy support and tuning for Intel T2 MacBook Pros.
 
 Usage:
-  omarchy-t2 setup [--defaults] [--yes] [--dry-run]
+  omarchy-t2 setup [--defaults] [--bluetooth] [--yes] [--dry-run]
   omarchy-t2 status
   omarchy-t2 doctor
   omarchy-t2 battery [status | limit <50-100>]
@@ -422,22 +422,22 @@ power_thermal() {
 }
 
 power_status() {
-  local live_profile=unavailable live_source=unknown live_state=unknown service=inactive
+  local live_profile=unavailable live_source=unknown live_state=unknown service
+  service=$(service_actual_state power-optimizer.service)
   load_system_config
   if runtime; then
-    systemctl is-active --quiet power-optimizer.service && service=active
     live_profile=$(powerprofilesctl get 2>/dev/null || echo unavailable)
     [[ -r /run/power-optimizer/power_source ]] && live_source=$(< /run/power-optimizer/power_source)
     [[ -r /run/power-optimizer/thermal_state ]] && live_state=$(< /run/power-optimizer/thermal_state)
   fi
   cat <<EOF
-enabled=$POWER_ENABLED
 service=$service
-profiles=$POWER_AC_PROFILE/$POWER_BATTERY_PROFILE automatic=$POWER_PROFILE_SWITCHING
-gpu-saving=$POWER_GPU_SAVING profile=$POWER_GPU_PROFILE
-wifi-saving=$POWER_WIFI_SAVING
-usb-autosuspend=$POWER_USB_AUTOSUSPEND
-thermal-idle=$POWER_THERMAL_IDLE range=${POWER_WARM_C}-${POWER_HOT_C}C idle=${POWER_WARM_IDLE_PERCENT}-${POWER_HOT_IDLE_PERCENT}% recovery=${POWER_WARM_RECOVERY_C}/${POWER_HOT_RECOVERY_C}C
+configured.enabled=$POWER_ENABLED
+configured.profiles=$POWER_AC_PROFILE/$POWER_BATTERY_PROFILE automatic=$POWER_PROFILE_SWITCHING
+configured.gpu-saving=$POWER_GPU_SAVING profile=$POWER_GPU_PROFILE
+configured.wifi-saving=$POWER_WIFI_SAVING
+configured.usb-autosuspend=$POWER_USB_AUTOSUSPEND
+configured.thermal-idle=$POWER_THERMAL_IDLE range=${POWER_WARM_C}-${POWER_HOT_C}C idle=${POWER_WARM_IDLE_PERCENT}-${POWER_HOT_IDLE_PERCENT}% recovery=${POWER_WARM_RECOVERY_C}/${POWER_HOT_RECOVERY_C}C
 live=source:$live_source profile:$live_profile thermal:$live_state
 EOF
 }
@@ -532,7 +532,7 @@ tts_models_ready() {
 
 tts_require_runtime() {
   local command
-  [[ -x $TTS_ENGINE ]] || die "packaged TTS engine is missing: $TTS_ENGINE"
+  [[ -x $TTS_ENGINE ]] || die "optional Qwen TTS engine is not installed: $TTS_ENGINE; install with WITH_QWEN_TTS=1 or set OMARCHY_T2_TTS_ENGINE to an existing Vulkan qwen-tts executable"
   for command in curl ffmpeg flock notify-send pw-play sha256sum wl-paste; do
     command -v "$command" >/dev/null 2>&1 || die "TTS dependency is missing: $command"
   done
@@ -746,21 +746,57 @@ touchbar_uninstall() {
   (cd "$TOUCHBAR_DIR" && ./uninstall.sh)
 }
 
+# Compatibility: restore backups made by earlier profile-cleanup versions.
+audio_restore_previous() {
+  local kind=$1 scope manifest profile
+  for scope in system user; do
+    manifest="$USER_STATE/audio-$kind-$scope.removed"
+    [[ -f $manifest ]] || continue
+    while IFS= read -r profile; do
+      [[ -n $profile ]] || continue
+      if [[ $scope == system ]]; then
+        restore_system_file "$profile"
+      else
+        restore_user_file "$profile"
+      fi
+    done <"$manifest"
+    rm -f -- "$manifest"
+  done
+}
+
+audio_restore_default_tuning() {
+  local manifest="$USER_STATE/audio-default-tuning.removed"
+  local state="$USER_STATE/audio-default-tuning.service-state"
+  local profile enabled active
+  [[ -f $state ]] || return 0
+  if [[ -f $manifest ]]; then
+    while IFS= read -r profile; do
+      [[ -n $profile ]] && restore_user_file "$profile"
+    done <"$manifest"
+  fi
+  user_systemctl daemon-reload
+  read -r enabled active <"$state"
+  if [[ $enabled == enabled ]]; then user_systemctl enable omarchy-speaker-tuning.service; fi
+  if [[ $active == active ]]; then user_systemctl start omarchy-speaker-tuning.service; fi
+  rm -f -- "$manifest" "$state"
+}
+
 audio_speakers_enable() {
   local local_bin="${XDG_BIN_HOME:-$HOME/.local/bin}"
-  local legacy="$ROOT/etc/pipewire/pipewire.conf.d/10-t2_161_speakers.conf"
   local target="$ROOT/etc/pipewire/pipewire.conf.d/10-omarchy-t2-speakers.conf"
-  if [[ -e $legacy || -L $legacy ]]; then
-    backup_system_file "$legacy"
-    run_root rm -f "$legacy"
-  fi
   replace_system_link "$SHARE_DIR/audio/10-omarchy-t2-speakers.conf" "$target"
   replace_user_link "$LIB_DIR/audio-output-sink" "$local_bin/omarchy-audio-output-sink"
 }
 
 audio_speakers_disable() {
   restore_system_file "$ROOT/etc/pipewire/pipewire.conf.d/10-omarchy-t2-speakers.conf"
-  restore_system_file "$ROOT/etc/pipewire/pipewire.conf.d/10-t2_161_speakers.conf"
+  audio_restore_previous speakers
+  audio_restore_default_tuning
+  # Also support backups created before migration manifests existed.
+  local legacy="$ROOT/etc/pipewire/pipewire.conf.d/10-t2_161_speakers.conf"
+  if [[ -e $(system_backup_path "$legacy") || -L $(system_backup_path "$legacy") ]]; then
+    restore_system_file "$legacy"
+  fi
   restore_user_file "${XDG_BIN_HOME:-$HOME/.local/bin}/omarchy-audio-output-sink"
 }
 
@@ -782,6 +818,7 @@ audio_mic_disable() {
   local config_home=${XDG_CONFIG_HOME:-$HOME/.config}
   user_systemctl disable --now omarchy-t2-mic-guard.service >/dev/null 2>&1 || true
   restore_user_file "$config_home/pipewire/pipewire.conf.d/10-omarchy-t2-mic.conf"
+  audio_restore_previous mic
   restore_user_file "$config_home/systemd/user/omarchy-mic-guard.service"
   user_systemctl daemon-reload
 }
@@ -816,6 +853,8 @@ bluetooth_enable() {
   local agent="$LIB_DIR/bluetooth-agent"
   local temporary
 
+  install -d -m0755 "$USER_STATE"
+  touch "$USER_STATE/bluetooth-managed"
   replace_user_link "$SHARE_DIR/wireplumber/bluetooth-a2dp-autoconnect.conf" "$config_home/wireplumber/wireplumber.conf.d/omarchy-t2-a2dp-autoconnect.conf"
   if [[ -e $legacy || -L $legacy ]]; then
     backup_user_file "$legacy"
@@ -843,11 +882,17 @@ EOF
 
 bluetooth_disable() {
   local config_home=${XDG_CONFIG_HOME:-$HOME/.config}
+  local dropin="$config_home/systemd/user/bt-agent.service.d/omarchy-t2.conf"
+  # Default setup must leave Bluetooth alone, including during restore.
+  if [[ ! -e $USER_STATE/bluetooth-managed ]] && ! grep -Fq '# Managed by omarchy-t2.' "$dropin" 2>/dev/null; then
+    return 0
+  fi
   restore_user_file "$config_home/systemd/user/bt-agent.service.d/omarchy-t2.conf"
   restore_user_file "$config_home/systemd/user/bt-agent.service.d/upstream-fix.conf"
   restore_user_file "$config_home/wireplumber/wireplumber.conf.d/omarchy-t2-a2dp-autoconnect.conf"
   user_systemctl daemon-reload
   user_systemctl restart bt-agent.service
+  rm -f "$USER_STATE/bluetooth-managed"
 }
 
 reset_to_package_defaults() {
@@ -858,11 +903,13 @@ reset_to_package_defaults() {
 cmd_setup() {
   local assume_yes=false
   local defaults=false
+  local with_bluetooth=false
 
   while (($#)); do
     case $1 in
     --yes|-y) assume_yes=true ;;
     --defaults) defaults=true ;;
+    --bluetooth) with_bluetooth=true ;;
     --dry-run) DRY_RUN=true ;;
     *) die "unknown setup option: $1" ;;
     esac
@@ -877,7 +924,7 @@ Omarchy T2 will configure $SUPPORTED_MODEL with:
   • adaptive AC/battery, Radeon, Wi-Fi, USB, and thermal power policy
   • Mac ISO keyboard layout with tap-to-click disabled
   • protected six-speaker DSP and normalized microphone DSP
-  • Bluetooth passkey display and A2DP auto-connect
+  • Bluetooth changes: $([[ $with_bluetooth == true ]] && echo enabled || echo skipped)
 
 Existing files are backed up under:
   $SYSTEM_STATE
@@ -907,7 +954,9 @@ EOF
   input_apply
   audio_speakers_enable
   audio_mic_enable
-  bluetooth_enable
+  if [[ $with_bluetooth == true ]]; then
+    bluetooth_enable
+  fi
   restart_audio
   audio_defaults
 
@@ -917,24 +966,108 @@ EOF
   fi
 }
 
+battery_actual_limit() {
+  local path value
+  path=$(find "$ROOT/sys/devices" -path '*/APP0001:00/battery_charge_limit' -print -quit 2>/dev/null || true)
+  if [[ -r $path ]]; then
+    value=$(<"$path")
+    [[ $value =~ ^[0-9]+$ ]] && { echo "$value"; return; }
+  fi
+  echo unavailable
+}
+
+service_actual_state() {
+  local state
+  if runtime && state=$(systemctl show "$1" --property=ActiveState --value 2>/dev/null); then
+    case $state in
+      active|inactive|failed|activating|deactivating|reloading) echo "$state"; return ;;
+    esac
+  fi
+  echo unavailable
+}
+
+fan_installed_profile() {
+  local profile
+  [[ -r $ROOT/etc/t2fand.conf ]] || { echo unavailable; return; }
+  for profile in cool balanced quiet; do
+    if cmp -s "$ROOT/etc/t2fand.conf" "$SHARE_DIR/fan/t2fand-$profile.conf"; then
+      echo "$profile"
+      return
+    fi
+  done
+  echo custom
+}
+
+hypr_actual_option() {
+  local json
+  if runtime && json=$(hyprctl -j getoption "$1" 2>/dev/null); then
+    if printf '%s' "$json" | python3 -c '
+import json, sys
+try:
+    data = json.load(sys.stdin)
+    value = data["str"] if "str" in data else data["bool"]
+    print(str(value).lower() if isinstance(value, bool) else value)
+except (ValueError, KeyError, TypeError):
+    sys.exit(1)
+' 2>/dev/null; then return; fi
+  fi
+  echo unavailable
+}
+
+input_actual_state() {
+  KEYBOARD_LAYOUT=$(hypr_actual_option input:kb_layout)
+  KEYBOARD_VARIANT=$(hypr_actual_option input:kb_variant)
+  TAP_TO_CLICK=$(hypr_actual_option input:touchpad:tap-to-click)
+  [[ $KEYBOARD_VARIANT == unavailable ]] && KEYBOARD_VARIANT=
+  return 0
+}
+
+tts_actual_state() {
+  local bindings
+  if runtime && bindings=$(hyprctl -j binds 2>/dev/null); then
+    if printf '%s' "$bindings" | python3 -c '
+import json, sys
+try:
+    bindings = json.load(sys.stdin)
+    if not isinstance(bindings, list):
+        sys.exit(1)
+    print("enabled" if any(b.get("arg") == "omarchy-tts read" for b in bindings) else "disabled")
+except (ValueError, AttributeError, TypeError):
+    sys.exit(1)
+' 2>/dev/null; then return; fi
+  fi
+  echo unavailable
+}
+
+audio_actual_state() {
+  local nodes
+  if runtime && nodes=$(pactl list short "$1" 2>/dev/null); then
+    if awk -v node="$2" '$2 == node { found=1 } END { exit !found }' <<<"$nodes"; then
+      echo active
+    else
+      echo inactive
+    fi
+  else
+    echo unavailable
+  fi
+}
+
 cmd_status() {
-  local actual_battery=unavailable
-  local battery_path
-  load_system_config
-  load_input_config
-  battery_path=$(find "$ROOT/sys/devices" -path '*/APP0001:00/battery_charge_limit' -print -quit 2>/dev/null || true)
-  [[ -r $battery_path ]] && actual_battery=$(<"$battery_path")
+  local actual_battery
+  actual_battery=$(battery_actual_limit)
+  [[ $actual_battery == unavailable ]] || actual_battery+='%'
+  input_actual_state
   cat <<EOF
 Model:          $(model_name)
-Battery limit:  $BATTERY_LIMIT% (hardware: $actual_battery)
-Fan profile:    $FAN_PROFILE
-Power policy:   $([[ $POWER_ENABLED == true ]] && echo enabled || echo disabled)
+Battery limit:  $actual_battery
+Fan profile:    $(fan_installed_profile) (installed config; service: $(service_actual_state t2fanrd.service))
+Power policy:   $(service_actual_state power-optimizer.service)
 Keyboard:       $KEYBOARD_LAYOUT${KEYBOARD_VARIANT:+/$KEYBOARD_VARIANT}
 Tap-to-click:   $TAP_TO_CLICK
-Text-to-speech: $([[ $TTS_ENABLED == true ]] && echo enabled || echo disabled) (models: $(tts_models_ready && echo ready || echo missing))
+Text-to-speech: $(tts_actual_state) (bindings; models: $(tts_models_ready && echo ready || echo missing); engine: $([[ -x $TTS_ENGINE ]] && echo ready || echo missing))
 Touch Bar UI:   $(touchbar_service_state) (source: $(touchbar_source_state))
-Speaker DSP:    $([[ -e $ROOT/etc/pipewire/pipewire.conf.d/10-omarchy-t2-speakers.conf ]] && echo enabled || echo disabled)
-Microphone DSP: $([[ -e ${XDG_CONFIG_HOME:-$HOME/.config}/pipewire/pipewire.conf.d/10-omarchy-t2-mic.conf ]] && echo enabled || echo disabled)
+Speaker DSP:    $(audio_actual_state sinks effect_input.filter-chain-speakers)
+Microphone DSP: $(audio_actual_state sources effect_output.filter-chain-t2-mic)
 EOF
 }
 
@@ -1033,7 +1166,7 @@ cmd_restore() {
 cmd_battery() {
   require_supported_model
   case ${1:-status} in
-  status) load_system_config; echo "$BATTERY_LIMIT" ;;
+  status) battery_actual_limit ;;
   limit) [[ $# == 2 ]] || die "usage: omarchy-t2 battery limit <50-100>"; battery_limit "$2" ;;
   *) die "usage: omarchy-t2 battery [status | limit <50-100>]" ;;
   esac
@@ -1043,8 +1176,7 @@ cmd_fan() {
   require_supported_model
   case ${1:-status} in
   status)
-    load_system_config
-    echo "profile=$FAN_PROFILE"
+    echo "profile=$(fan_installed_profile) (installed config)"
     if runtime; then "$LIB_DIR/fan-control" status; fi
     ;;
   profile) [[ $# == 2 ]] || die "usage: omarchy-t2 fan profile <cool|balanced|quiet>"; fan_profile "$2" ;;
@@ -1075,7 +1207,7 @@ cmd_power() {
 cmd_input() {
   require_supported_model
   case ${1:-status} in
-  status) load_input_config; printf 'keyboard=%s/%s tap=%s\n' "$KEYBOARD_LAYOUT" "$KEYBOARD_VARIANT" "$TAP_TO_CLICK" ;;
+  status) input_actual_state; printf 'keyboard=%s/%s tap=%s\n' "$KEYBOARD_LAYOUT" "$KEYBOARD_VARIANT" "$TAP_TO_CLICK" ;;
   keyboard) (($# >= 2 && $# <= 3)) || die "usage: omarchy-t2 input keyboard <layout> [variant]"; input_keyboard "$2" "${3:-}" ;;
   tap) [[ $# == 2 ]] || die "usage: omarchy-t2 input tap <on|off>"; input_tap "$2" ;;
   *) die "usage: omarchy-t2 input [status | keyboard <layout> [variant] | tap <on|off>]" ;;
@@ -1162,9 +1294,8 @@ cmd_tts() {
   require_supported_model
   case ${1:-status} in
   status)
-    load_input_config
-    printf 'enabled=%s models=%s engine=%s\n' \
-      "$TTS_ENABLED" \
+    printf 'bindings=%s models=%s engine=%s\n' \
+      "$(tts_actual_state)" \
       "$(tts_models_ready && echo ready || echo missing)" \
       "$([[ -x $TTS_ENGINE ]] && echo ready || echo missing)"
     ;;
diff --git i/libexec/omarchy-tts w/libexec/omarchy-tts
index 6ab594c..f124af5 100755
--- i/libexec/omarchy-tts
+++ w/libexec/omarchy-tts
@@ -110,7 +110,11 @@ finish_job() {
 }
 
 require_tts() {
-  if [[ ! -x $synthesizer || ! -f $model || ! -f $codec ]]; then
+  if [[ ! -x $synthesizer ]]; then
+    notify-send -u critical "Read selected text" "Optional Qwen TTS engine is not installed; see omarchy-t2 documentation"
+    exit 1
+  fi
+  if [[ ! -f $model || ! -f $codec ]]; then
     notify-send -u critical "Read selected text" "Run 'omarchy-t2 tts setup' first"
     exit 1
   fi
diff --git i/omarchy-t2.install w/omarchy-t2.install
index 0fb5efc..8fbc212 100644
--- i/omarchy-t2.install
+++ w/omarchy-t2.install
@@ -4,7 +4,7 @@ post_install() {
   echo "Preview first with 'omarchy-t2 setup --dry-run'."
   echo "Inspect or personalize power policy with 'omarchy-t2 power status'."
   echo "Optional: run 'omarchy-t2 touchbar setup' for the React DRM Touch Bar."
-  echo "Optional: run 'omarchy-t2 tts setup' for Vulkan text-to-speech."
+  echo "Optional: install a Qwen TTS engine, then run 'omarchy-t2 tts setup'."
   echo
 }
 
@@ -13,6 +13,6 @@ post_upgrade() {
   echo "Run 'omarchy-t2 doctor' after upgrading to verify hardware support."
   echo "Inspect or personalize power policy with 'omarchy-t2 power status'."
   echo "Optional: run 'omarchy-t2 touchbar setup' for the React DRM Touch Bar."
-  echo "Optional: run 'omarchy-t2 tts setup' for Vulkan text-to-speech."
+  echo "Optional: install a Qwen TTS engine, then run 'omarchy-t2 tts setup'."
   echo
 }
diff --git 1/tests/test-status.sh 2/tests/test-status.sh
new file mode 100644
index 0000000..759f062
--- /dev/null
+++ 2/tests/test-status.sh
@@ -0,0 +1,78 @@
+#!/bin/bash
+set -euo pipefail
+project_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
+test_dir=$(mktemp -d)
+trap 'rm -rf "$test_dir"' EXIT
+export OMARCHY_T2_ROOT="$test_dir/root"
+export HOME="$test_dir/home"
+export XDG_CONFIG_HOME="$HOME/.config"
+export XDG_DATA_HOME="$HOME/.local/share"
+export XDG_STATE_HOME="$HOME/.local/state"
+mkdir -p "$OMARCHY_T2_ROOT/etc" "$OMARCHY_T2_ROOT/sys/devices/platform/APP0001:00" "$XDG_CONFIG_HOME/omarchy-t2"
+printf 'BATTERY_LIMIT=95\nFAN_PROFILE=cool\nPOWER_ENABLED=true\n' >"$OMARCHY_T2_ROOT/etc/omarchy-t2.conf"
+printf 'KEYBOARD_LAYOUT=de\nTAP_TO_CLICK=false\nTTS_ENABLED=true\n' >"$XDG_CONFIG_HOME/omarchy-t2/config"
+printf '100\n' >"$OMARCHY_T2_ROOT/sys/devices/platform/APP0001:00/battery_charge_limit"
+cp "$project_dir/config/t2fand-quiet.conf" "$OMARCHY_T2_ROOT/etc/t2fand.conf"
+source "$project_dir/bin/omarchy-t2" help >/dev/null
+require_supported_model() { return 0; }
+mkdir -p "$test_dir/share/fan"
+cp "$project_dir"/config/t2fand-*.conf "$test_dir/share/fan/"
+SHARE_DIR="$test_dir/share"
+# Mock only read-only runtime queries; never access the host session.
+runtime() { return 0; }
+query_failure=false
+live_service=inactive
+live_tts=false
+live_audio=false
+systemctl() {
+  [[ $query_failure == false && $1 == show ]] || return 1
+  echo "$live_service"
+}
+hyprctl() {
+  [[ $query_failure == false ]] || return 1
+  case "$2 ${3:-}" in
+    'getoption input:kb_layout') echo '{"str":"us"}' ;;
+    'getoption input:kb_variant') echo '{"str":"mac-iso"}' ;;
+    'getoption input:touchpad:tap-to-click') echo '{"bool":true}' ;;
+    'binds ') if $live_tts; then echo '[{"arg":"omarchy-tts read"}]'; else echo '[]'; fi ;;
+    *) return 1 ;;
+  esac
+}
+pactl() {
+  [[ $query_failure == false ]] || return 1
+  if $live_audio; then
+    echo '1 effect_input.filter-chain-speakers PipeWire SUSPENDED'
+    echo '2 effect_output.filter-chain-t2-mic PipeWire SUSPENDED'
+  fi
+}
+assert_line() { grep -Fxq "$1" <<<"$status" || { echo "Missing: $1" >&2; echo "$status" >&2; exit 1; }; }
+status=$(cmd_status)
+assert_line 'Battery limit:  100%'
+assert_line 'Fan profile:    quiet (installed config; service: inactive)'
+assert_line 'Power policy:   inactive'
+assert_line 'Keyboard:       us/mac-iso'
+assert_line 'Tap-to-click:   true'
+assert_line 'Text-to-speech: disabled (bindings; models: missing; engine: missing)'
+assert_line 'Speaker DSP:    inactive'
+assert_line 'Microphone DSP: inactive'
+[[ $(cmd_battery status) == 100 ]]
+[[ $(cmd_input status) == 'keyboard=us/mac-iso tap=true' ]]
+live_service=active
+live_tts=true
+live_audio=true
+status=$(cmd_status)
+assert_line 'Power policy:   active'
+assert_line 'Text-to-speech: enabled (bindings; models: missing; engine: missing)'
+assert_line 'Speaker DSP:    active'
+assert_line 'Microphone DSP: active'
+query_failure=true
+status=$(cmd_status)
+assert_line 'Power policy:   unavailable'
+assert_line 'Keyboard:       unavailable'
+assert_line 'Tap-to-click:   unavailable'
+assert_line 'Speaker DSP:    unavailable'
+# Missing hardware must not fall back to the stale saved limit.
+rm "$OMARCHY_T2_ROOT/sys/devices/platform/APP0001:00/battery_charge_limit"
+[[ $(battery_actual_limit) == unavailable ]]
+echo 'All live status tests passed'
+
